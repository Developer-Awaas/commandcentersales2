logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
vite.config.ts.timestamp-*.mjs

# Benchmark output (generated images, results) — regenerate via
# `deno run -A benchmark/image-providers.ts`, don't commit run artifacts.
benchmark/output/

# Local secrets file — never commit, never had git history (verified 2026-07-20)
_secrets.json

# A/B test scratch artifacts (GPT-Image-1 currency-symbol investigation)
.ab-test-tmp/

# Playwright output — regenerate via `npm run test:e2e`, don't commit run artifacts.
test-results/
playwright-report/

# Playwright monitor screenshots (regenerated in CI, uploaded as an artifact)
screenshots/

# offline overlay replay harness outputs (regenerate via scripts/replay-composite.ts)
scripts/replay-out/
