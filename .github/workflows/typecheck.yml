name: Type-check & Unit Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Client build (TypeScript + Vite)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npm run typecheck

      - run: npm run build

  client-unit-tests:
    name: Client unit tests (Vitest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Node 22 (not 20 like the build job) specifically because the Vitest
      # jsdom environment loads undici@8.9.0 (jsdom@30's transitive dep),
      # which declares engines.node ">=22.19.0" and hard-crashes on Node 20
      # with "webidl.util.markAsUncloneable is not a function" before any
      # test runs. Only this job needs the bump — the build job doesn't load
      # jsdom, so it stays on 20. (This is the same undici engine mismatch
      # first spotted as a warning during the PR #17 lockfile investigation.)
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - run: npm test

  edge-typecheck:
    name: Edge Function type check (Deno)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Cache Deno modules
        uses: actions/cache@v4
        with:
          path: ~/.deno
          key: deno-${{ runner.os }}-${{ hashFiles('supabase/functions/**/*.ts', 'supabase/functions/deno.json', 'supabase/functions/deno.lock') }}
          restore-keys: deno-${{ runner.os }}-

      # Explicit deno install, not left to nodeModulesDir:auto lazy
      # trigger during check/test - that lazy path reproducibly failed on
      # Linux CI (Could not find a matching package for npm:@types/node)
      # while never reproducing locally under any invocation tried. A
      # dedicated install step removes the ambiguity about when/whether
      # auto-install actually fires.
      - name: deno install
        run: deno install --lock=supabase/functions/deno.lock
        working-directory: supabase/functions

      # supabase/functions/deno.json is picked up automatically (Deno walks up
      # from each file's directory to find the nearest config file).
      # --lock is explicit, not left to auto-detection: without it, an
      # unpinned esm.sh/@supabase/supabase-js@2 specifier can resolve to
      # whatever esm.sh serves that request (observed drifting across
      # 2.108.2/2.110.7 within the same day) even with deno.lock sitting
      # right next to deno.json - --lock forces the pinned redirect.
      - name: deno check — all Edge Function entry points
        run: |
          deno check \
            --lock=supabase/functions/deno.lock \
            supabase/functions/aarav-orchestrate/index.ts \
            supabase/functions/canva-oauth-callback/index.ts \
            supabase/functions/canva-open-editor/index.ts \
            supabase/functions/canva-connect-init/index.ts \
            supabase/functions/canva-sync-design/index.ts \
            supabase/functions/canva-verify-return-nav/index.ts \
            supabase/functions/generate-image/index.ts \
            supabase/functions/langfuse-ingest/index.ts \
            supabase/functions/meta-insights-sync/index.ts \
            supabase/functions/claude-proxy/index.ts \
            supabase/functions/dhruv-anomaly-check/index.ts \
            supabase/functions/history-retention-sweep/index.ts \
            supabase/functions/ingest-review/index.ts \
            supabase/functions/meta-oauth-start/index.ts \
            supabase/functions/meta-oauth-callback/index.ts \
            supabase/functions/meta-token-connect/index.ts \
            supabase/functions/meta-sync-now/index.ts \
            supabase/functions/dhruv-weekly-report/index.ts

  edge-unit-tests:
    name: Edge Function unit tests (no credentials)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Cache Deno modules
        uses: actions/cache@v4
        with:
          path: ~/.deno
          key: deno-${{ runner.os }}-${{ hashFiles('supabase/functions/**/*.ts', 'supabase/functions/deno.json', 'supabase/functions/deno.lock') }}
          restore-keys: deno-${{ runner.os }}-

      - name: deno install
        run: deno install --lock=supabase/functions/deno.lock
        working-directory: supabase/functions

      # Confirmed root cause (2026-07-21, via a temporary diagnostic step -
      # see git history for commits 6279f60/498e705 if this ever needs
      # re-diagnosing): deno check (works) always targets files directly
      # inside supabase/functions/, walking up from each file's own
      # directory to find deno.json/deno.lock. deno test previously
      # targeted a directory from repo root - one level removed from where
      # deno install (working-directory: supabase/functions) actually
      # populated node_modules - and its directory-target config discovery
      # never resolved against that tree on this Linux runner, even though
      # deno install had installed @types/node correctly seconds earlier
      # and its symlink was fully intact (both confirmed via the removed
      # diagnostic's output - this was not a cache, install, or symlink
      # problem, all three were separately ruled out with direct evidence).
      # Fix: run deno test from the same working directory deno install
      # populated, with paths relative to it.
      - name: deno test — credential-free unit tests
        run: |
          deno test --allow-env \
            --lock=deno.lock \
            _shared/
        working-directory: supabase/functions

  ws1-6-isolation:
    name: WS1.6 isolation harness (agent_memory_chunks)
    runs-on: ubuntu-latest
    # Secrets are scoped to THIS job only via job-level env — never promoted
    # to workflow-level env, so client-unit-tests/build/edge-typecheck/
    # edge-unit-tests above never see SUPABASE_SERVICE_ROLE_KEY. Runs
    # against the only Supabase project this repo has (CommandCentre_Prod —
    # there is no separate TEST project); every row seeded is identifiably
    # prefixed and removed by the cleanup step below regardless of outcome.
    #
    # Requires these repo secrets (Settings -> Secrets and variables ->
    # Actions) — none exist yet as of this job being added:
    #   REST_BASE, ANON_KEY, SUPABASE_SERVICE_ROLE_KEY,
    #   PROBE_ORG_A_EMAIL, PROBE_ORG_A_PASSWORD,
    #   PROBE_ORG_B_EMAIL, PROBE_ORG_B_PASSWORD
    # Until they're set, every step below fails fast on `mustEnv`/`:?not set`
    # — a clear, unambiguous failure, not a silent skip.
    #
    # Probe 7 will fail even once secrets exist: it depends on
    # aarav-orchestrate actually implementing the Phase 5 approve dispatch,
    # which is not currently confirmed deployed (see CLAUDE.md "Phase 5").
    # That's accurate signal, not a broken job — do not silence it with
    # continue-on-error.
    env:
      REST_BASE: ${{ secrets.REST_BASE }}
      ANON_KEY: ${{ secrets.ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      PROBE_ORG_A_EMAIL: ${{ secrets.PROBE_ORG_A_EMAIL }}
      PROBE_ORG_A_PASSWORD: ${{ secrets.PROBE_ORG_A_PASSWORD }}
      PROBE_ORG_B_EMAIL: ${{ secrets.PROBE_ORG_B_EMAIL }}
      PROBE_ORG_B_PASSWORD: ${{ secrets.PROBE_ORG_B_PASSWORD }}
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: seed isolation probes (idempotent — safe on repeat CI runs)
        run: bash supabase/tests/isolation/seed-isolation-probes.sh

      - name: run isolation probe suite (fail-closed)
        run: |
          deno test --allow-net --allow-env --allow-read \
            supabase/tests/isolation/isolation_test.ts

      # Always runs, even if the probe suite above failed, so a failing
      # probe never leaves seeded orgs/users/rows behind in prod.
      - name: cleanup isolation probes
        if: always()
        run: bash supabase/tests/isolation/cleanup-isolation-probes.sh

  playwright-e2e:
    name: Playwright e2e (mock AI, ZZ-INTERNAL-TEST)
    runs-on: ubuntu-latest
    # Advisory, NOT a required check (see branch protection): this touches
    # PROD (writes scoped to ZZ-INTERNAL-TEST) and can flake on network — a
    # red run here should inform, not block a merge, until it's proven
    # stable. Secrets are job-level only (never workflow-level), mirroring
    # ws1-6-isolation, so the other jobs never see the service-role key.
    #
    # INTERNAL_TEST_USER_* are the ZZ-INTERNAL-TEST org's dedicated login
    # (P0 Step 6). VITE_SUPABASE_URL/ANON_KEY are baked into the preview
    # build so the app can reach PROD; every write the test makes is scoped
    # to ZZ-INTERNAL-TEST by that login. SUPABASE_SERVICE_ROLE_KEY is used
    # ONLY by the seed/cleanup scripts (bypass RLS to prepare + tear down
    # the dedicated ZZ-E2E project's data), never by the app under test.
    #
    # The spec self-skips when INTERNAL_TEST_USER_* are absent (see the
    # file header) — so if those secrets aren't set this job passes green
    # with "1 skipped" rather than failing.
    env:
      SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
      VITE_SUPABASE_ANON_KEY: ${{ secrets.ANON_KEY }}
      INTERNAL_TEST_USER_EMAIL: ${{ secrets.INTERNAL_TEST_USER_EMAIL }}
      INTERNAL_TEST_USER_PASSWORD: ${{ secrets.INTERNAL_TEST_USER_PASSWORD }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      # Idempotent — ensures the dedicated ZZ-E2E project exists before the
      # wizard needs to select it.
      - name: seed e2e project (ZZ-INTERNAL-TEST)
        run: node e2e/seed-e2e.mjs

      - run: npm run test:e2e

      # Always runs, even on test failure, so a mid-flow crash never leaves
      # campaigns / tool_outputs / creative_assets behind on prod.
      - name: cleanup e2e data
        if: always()
        run: node e2e/cleanup-e2e.mjs

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      # Full-page Monitor screenshots (populated when ZZ-INTERNAL-TEST demo
      # metrics are seeded) — downloadable from the run's Artifacts.
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: monitor-screenshots
          path: screenshots/
          retention-days: 14
          if-no-files-found: ignore
